# Jarvis Trader - Flutter Mobile App

This is a minimal Flutter starter for Jarvis Trader.

Quick start:

1. Install Flutter SDK: https://flutter.dev/docs/get-started/install
2. From this folder run: `flutter pub get` then `flutter run`.

Notes:
- This app demonstrates a basic UI and will later integrate with the Jarvis API endpoints.
